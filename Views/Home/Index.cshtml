@model List<InternetProg.Models.SliderResim>
@{
    ViewBag.Title = "Ana Sayfa";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div id="mainCarousel" class="carousel slide shadow-lg mb-5 mx-auto rounded-4 overflow-hidden" data-bs-ride="carousel" style="max-width: 1200px;">
    <div class="carousel-inner">
        @if (Model != null && Model.Count > 0)
        {
            for (int i = 0; i < Model.Count; i++)
            {
                <div class="carousel-item @(i == 0 ? "active" : "")">
                    <img src="@Model[i].ResimUrl" class="d-block w-100" alt="Etiyopya Slider"
                         style="aspect-ratio: 21/9; object-fit: cover;">
                </div>
            }
        }
        else
        {
            <div class="carousel-item active">
               
            </div>
        }
    </div>

    <button class="carousel-control-prev" type="button" data-bs-target="#mainCarousel" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Önceki</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#mainCarousel" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Sonraki</span>
    </button>
</div>

<div class="container text-center mb-5" style="max-width: 900px;">

    <h1 class="display-5 fw-bold mb-3" style="font-family: 'Playfair Display', serif; color: #1a4d2e;">Etiyopya'ya Hoşgeldiniz</h1>

    <div class="d-flex justify-content-center mb-4">
        <div style="height: 3px; width: 30px; background-color: #009A44; margin-right: 4px;"></div>
        <div style="height: 3px; width: 30px; background-color: #FEDD00; margin-right: 4px;"></div>
        <div style="height: 3px; width: 30px; background-color: #FF0000;"></div>
    </div>

    <p class="lead text-secondary mb-4" style="line-height: 1.8; font-size: 1.05rem;">
        Afrika Boynuzu'nun en eski bağımsız ülkesi olan Etiyopya, tarih boyunca "Habeşistan" olarak anılmış köklü bir medeniyettir.
        Kahvenin ana vatanı olan bu topraklar; UNESCO Dünya Mirası listesindeki 9 eseri, kendine özgü alfabesi, takvimi
        ve 3000 yılı aşan tarihiyle Afrika'nın kültür başkentidir. Doğal güzellikleri ve eşsiz tarihi dokusuyla
        sizi benzersiz bir keşfe davet ediyor.
    </p>

    <div class="p-3 bg-white rounded shadow-sm border border-light d-inline-block">
        <p class="mb-0 small text-muted">
            <i class="bi bi-info-circle-fill text-warning me-2"></i>
            Detaylı nüfus bilgileri ve şehirler için
            <a style="text-decoration: none; color: #1a4d2e; font-weight: bold;" href="/Home/Bilgiler">"Ülke Bilgileri"</a> sayfasını inceleyebilirsiniz.
        </p>
    </div>

</div>